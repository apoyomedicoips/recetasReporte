# Dashboard de Recetas IPS (versión mínima)

Estructura preparada para GitHub Pages. Generar los JSON a partir del CSV de recetas con:

```bash
python scripts/preparar_datos.py
```

Los archivos se guardan en `docs/data/` y son consumidos por `docs/index.html`.
